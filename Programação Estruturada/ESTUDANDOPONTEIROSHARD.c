#include <stdio.h>
#include <locale.h>

int main (){
	
	setlocale (LC_ALL, "");
	
	int x, y = 10, w, z[5], a;
	int *pont;
	
	for (a=0; a<5; a++){
	printf ("Digite %d° salario= ", a+1);
	scanf ("%d", &z[a]);
	
	w = z[a]+ z[a]+ z[a]+ z[a]+ z[a];}
	
	pont = &w;
	*pont == 7;
	
	
	printf ("soma dos salarios %d", w);
	
}
